# 🔧 Скрипты и утилиты проекта

Здесь находятся вспомогательные Python скрипты для работы с проектом.

---

## 📄 Содержание

### import_products.py
**Назначение:** Импорт товаров из CSV или Excel файлов в БД

**Использование:**
```bash
# Импорт из CSV файла
python scripts/import_products.py ../data/products_template.csv

# Импорт из Excel файла  
python scripts/import_products.py ../data/products_template.xlsx
```

**Возможности:**
- ✅ Поддержка CSV и XLSX форматов
- ✅ Автоматическое определение кодировки (UTF-8, CP1251, Latin-1, ISO-8859-5)
- ✅ Гибкие названия колонок
- ✅ Обновление товаров по SKU
- ✅ Подробные отчеты об ошибках

**Требуемые поля в файле:**
- `name` или `наименование` - Название товара
- `price` или `цена` - Цена
- `sku` или `артикул` - Артикул (уникален)
- `quantity` или `количество` - Количество (опционально)
- `description` или `описание` - Описание (опционально)

---

### create_xlsx_template.py
**Назначение:** Создание шаблона Excel файла с правильной структурой

**Использование:**
```bash
python scripts/create_xlsx_template.py
```

**Результат:**
- Создает файл `products_template.xlsx` с примером товаров
- Автоматическое форматирование
- Готов к использованию для импорта

**Содержит примеры:**
- 5 товаров с полной информацией
- Правильное оформления (шрифты, ширина колонок)
- Инструкции в первой строке

---

## 📋 Форматы файлов

### CSV (Comma-Separated Values)

**Пример структуры:**
```csv
name,price,sku,quantity,description
Товар 1,1500,SKU001,100,Описание товара 1
Товар 2,2500,SKU002,50,Описание товара 2
```

**Кодировка:** UTF-8 (или автоматически определяется)

**Разделитель:** Запятая (,) или точка с запятой (;)

### Excel (XLSX)

**Структура:**
- Первая строка - названия колонок
- Остальные строки - данные товаров
- Поддерживаются все типы данных (текст, числа, даты)

**Преимущества Excel:**
- Удобный редактор
- Форматирование
- Мультилистовые файлы

---

## 🚀 Примеры использования

### Импортировать пример товаров
```bash
# Перейти в папку back
cd d:\Test_Django_1\example\back

# Активировать виртуальное окружение
venv\Scripts\activate

# Запустить импорт
python scripts/import_products.py data/products_template.csv
```

### Создать новый шаблон
```bash
# Создать шаблон
python scripts/create_xlsx_template.py

# Файл сохранится в текущей папке
# Можно открыть в Excel и отредактировать
```

### Импортировать собственные товары
```bash
# 1. Подготовить файл (CSV или XLSX)
# 2. Запустить скрипт
python scripts/import_products.py path/to/your/file.csv

# 3. Проверить результат в DBeaver или админ-панели
```

---

## ⚙️ Технические детали

### Автоматическое определение кодировки

Скрипт `import_products.py` поддерживает несколько кодировок:
1. **UTF-8** - современный стандарт
2. **CP1251** - Windows кириллица
3. **Latin-1** - Западноевропейская
4. **ISO-8859-5** - Кириллица (ISO)

Выбор происходит автоматически!

### Обновление товаров

Если товар с таким SKU уже существует:
- Цена обновляется
- Описание обновляется
- Количество обновляется
- SKU не может быть изменен (это идентификатор)

### Обработка ошибок

Для каждой строки файла указывается:
- Номер строки
- Какое выполнено действие (создано/обновлено)
- Сам товар (SKU, name)
- Если ошибка - описание проблемы

---

## 📝 Примеры файлов

Примеры файлов находятся в папке `data/`:

```
data/
├── products_template.csv   # CSV пример
└── products_template.xlsx  # Excel пример
```

Используйте их как основу для своих файлов!

---

## ✅ Контрольный список

- [ ] Скрипты находятся в папке `scripts/`
- [ ] Примеры данных находятся в папке `data/`
- [ ] Виртуальное окружение активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] PostgreSQL запущена
- [ ] Миграции выполнены (`python manage.py migrate`)
- [ ] Готово импортировать товары!

---

**Последнее обновление:** 2026-02-20  
**Версия:** 1.0
