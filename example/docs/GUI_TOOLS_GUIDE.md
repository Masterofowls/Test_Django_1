# 🖥️ Работа с GUi инструментами для SQL

## 1️⃣ DBeaver (рекомендуется)

### Установка
1. Скачайте [DBeaver Community](https://dbeaver.io/download/) (бесплатная)
2. Установите приложение
3. Запустите DBeaver

### Подключение к базе данных

#### Способ 1: Создать новое подключение (подробно)

1. **Меню → Database → New Database Connection**
2. Выберите **PostgreSQL** и нажмите **Next**
3. Заполните параметры:

| Параметр | Значение |
|----------|----------|
| **Server Host** | localhost |
| **Port** | 5432 |
| **Database** | shop |
| **Username** | postgres |
| **Password** | postgres |
| **Save password** | ✓ (галочка) |

4. Нажмите **Test Connection** (должна появиться зеленая галочка)
5. Нажмите **Finish**

#### Способ 2: Быстрое подключение

1. Слева в панели **Database → postgres** (если уже добавлен)
2. Если нет, нажмите кнопку **+ (плюс)** рядом с Database
3. Выберите PostgreSQL

### Навигация в DBeaver

```
Database Navigator (левая панель)
├── postgres (сервер)
│   ├── Databases
│   │   ├── shop (наша база)
│   │   │   ├── Schemas
│   │   │   │   ├── public
│   │   │   │   │   ├── Tables
│   │   │   │   │   │   ├── auth_user
│   │   │   │   │   │   ├── bodies_product
│   │   │   │   │   │   ├── bodies_order
│   │   │   │   │   │   ├── bodies_profile
│   │   │   │   │   │   ├── bodies_pickuppoint
│   │   │   │   │   │   └── bodies_order_products
│   │   │   │   │   ├── Views
│   │   │   │   │   └── Functions
```

### Просмотр данных в таблице

1. **Database Navigator** → разверните `shop` → `Schemas` → `public` → `Tables`
2. Найдите таблицу (например `bodies_product`)
3. **Правый клик** → **View Data** → **View Data**
4. Откроется таблица с данными

### Выполнение SQL запросов

#### Способ 1: SQL Editor
1. **Меню → SQL Editor → New SQL Script** (или Ctrl+Alt+N)
2. Выберите соединение (shop)
3. Напишите запрос:
```sql
SELECT * FROM bodies_product LIMIT 10;
```
4. Нажмите **Execute** (или Ctrl+Enter)
5. Результаты появятся внизу

#### Способ 2: В контексте таблицы
1. Разверните таблицу в Database Navigator
2. Правый клик на таблице
3. **Generate SQL** → **SELECT** (или другой вариант)
4. Откроется SQL Editor с готовым запросом

### Редактирование данных прямо в таблице

1. Откройте таблицу (**View Data**)
2. Кликните на ячейку
3. Отредактируйте значение
4. Нажмите **Enter**
5. Изменения сохранятся автоматически

### Добавление новой записи

1. В табличном виде нажмите **плюс (+)** внизу
2. Заполните поля
3. Сохранится автоматически

### Удаление записи

1. Выберите строку (строка подсветится)
2. Нажмите **Delete** или **правый клик → Delete Row**
3. Подтвердите

### Встроенный инструмент от DBeaver

Нажмите на таблицу правый клик:

- **Query Rows** - быстрый SELECT все
- **View Data** - просмотр данных
- **Filters** - добавить фильтры
- **Sort** - добавить сортировку
- **Generate SQL** - генерировать SQL
- **Export** - экспортировать в CSV, JSON и т.д.

### Экспорт данных

1. Откройте таблицу
2. **Меню → Export Data**
3. Выберите формат (CSV, JSON, Excel и т.д.)
4. Укажите путь сохранения
5. Нажмите **Finish**

### Импорт данных

1. **Меню → Import Data**
2. Выберите файл (CSV, JSON, Excel)
3. Выберите таблицу назначения
4. Нажмите **Proceed**

---

## 2️⃣ pgAdmin (альтернатива)

### Установка

1. Скачайте [pgAdmin](https://www.pgadmin.org/download/) (бесплатный)
2. Установите
3. Запустите (откроется в браузере http://localhost:5050)
4. Логин по умолчанию: `pgadmin4@pgadmin.org` / `admin`

### Первичная настройка

При первом запуске:
1. **Master Password** - установите пароль (например: admin)
2. Нажмите **OK**

### Подключение к базе данных

1. Слева: **Servers** (правый клик)
2. **Create → Server**
3. Заполните:
   - **Name:** shop (или любое имя)
   - **Connection** tab:
     - **Host name/address:** localhost
     - **Port:** 5432
     - **Username:** postgres
     - **Password:** postgres
   - **Save password:** ✓
4. Нажмите **Save**

### Навигация в pgAdmin

```
Servers
├── shop
│   ├── Databases
│   │   ├── postgres
│   │   ├── shop (НАША!)
│   │   │   ├── Schemas
│   │   │   │   ├── public
│   │   │   │   │   ├── Tables
│   │   │   │   │   │   ├── auth_user
│   │   │   │   │   │   ├── bodies_*
│   │   │   │   │   └── Views
```

### Просмотр данных

1. **shop** → **Schemas** → **public** → **Tables** → выберите таблицу
2. Правый клик → **View/Edit Data** → **All Rows**
3. Откроется просмотр в браузере

### Выполнение SQL

1. Правый клик на БД **shop**
2. **Query Tool** (или нажмите на БД и выберите Tools → Query Tool)
3. Откроется SQL редактор
4. Напишите запрос
5. Нажмите **Execute** (F5)

---

## 3️⃣ VS Code с расширением

### Установка расширений

1. **Extensions** (Ctrl+Shift+X)
2. Найдите `PostgreSQL`
3. Установите расширение от `Chris Kolkman`

### Подключение в VS Code

1. нажмите **Ctrl+Shift+P** (Command Palette)
2. Напишите `PostgreSQL: New Connection`
3. Следуйте инструкциям:
   - Host: localhost
   - Port: 5432
   - User: postgres
   - Database: shop
   - Password: postgres

### Работа с запросами

1. Создайте файл `.sql` (например `query.sql`)
2. Напишите запрос
3. **Ctrl+Shift+P** → `PostgreSQL: Execute Query`
4. Результат появится в интегрированном терминале

---

## 4️⃣ Встроенный инструмент Django

### Просмотр через Django Shell

```bash
cd d:\Test_Django_1\example

# Запустить интерактивную оболочку Django
python manage.py shell
```

```python
# В интерпретаторе:
from bodies.models import Product, Order, Profile

# Получить все товары
products = Product.objects.all()
for p in products:
    print(f"{p.name} - {p.price} р.")

# Добавить товар
Product.objects.create(name='iPhone 15', price=99999.99, sku='IPHONE15', description='Новый')

# Получить заказы пользователя
user_orders = Order.objects.filter(user__username='testuser')

# Выход
exit()
```

---

## 5️⃣ Командная строка PostgreSQL

### Подключение
```bash
psql -U postgres -d shop -h localhost
```

### Основные команды

```sql
-- Список таблиц
\dt

-- Структура таблицы
\d bodies_product

-- Выполнить запрос
SELECT * FROM bodies_product LIMIT 5;

-- Сохранить результат в файл
\o output.txt
SELECT * FROM bodies_product;
\o  -- закончить сохранение

-- Выход
\q
```

---

## 🔧 РЕКОМЕНДУЕМЫЙ РАБОЧИЙ ПРОЦЕСС

### Для просмотра данных
**→ DBeaver** (самый удобный UI)

### Для редактирования данных
**→ DBeaver** (встроенные формы редактирования)

### Для импорта CSV
**→ DBeaver** (интегрированный импорт)

### Для выполнения сложных запросов
**→ DBeaver или pgAdmin** (оба имеют хороший SQL Editor)

### Для автоматизации
**→ Python скрипты** или **консоль psql**

### Для разработки Django
**→ Django Shell** (работает с моделями)

---

## 📌 ПОЛЕЗНЫЕ СОВЕТЫ

### 1. Задать темную тему в DBeaver
**Preferences** → **Appearance** → **Theme** → **Dark**

### 2. Автоформатирование SQL в DBeaver
Выделите текст → **Ctrl+Shift+F**

### 3. История запросов в DBeaver
**Window** → **Show View** → **Query Manager**

### 4. Закладки в DBeaver
**Меню File** → **Bookmark Manager** - сохраняйте частые запросы

### 5. Быстрый запрос в DBeaver
Выделите текст в SQL Editor → **Ctrl+Enter** (выполнится только выделенный)

### 6. DBeaver - экспорт в Excel
Откройте результаты → Правый клик → **Export** → **CSV/Excel**

### 7. Ограничить выборку результатов
```sql
-- Первые 100 строк
SELECT * FROM bodies_product LIMIT 100;
```

---

## ⚠️ ВНИМАНИЕ

### Никогда не:
1. ❌ Удаляйте таблицы напрямую (используйте миграции Django)
2. ❌ Редактируйте таблицы `django_*` вручную
3. ❌ Меняйте типы данных без бэкапа
4. ❌ Удаляйте пользователей если они создали заказы (каскадно удалится всё)

### Всегда:
1. ✅ Делайте бэкап перед крупными изменениями
2. ✅ Используйте Django миграции для изменений схемы
3. ✅ Проверяйте запрос на тестовых данных перед выполнением
4. ✅ Используйте SELECT перед DELETE для проверки

---

## 🆘 ЕСЛИ ЧТО-ТО СЛОМАЛОСЬ

### БД не от отвечает
```bash
# Проверить статус PostgreSQL
# Windows: Services → postgresql-... → Start

# Или в PowerShell:
pg_isready -h localhost -p 5432
```

### Не удается подключиться
1. Проверьте, что PostgreSQL запущена
2. Проверьте правильность пароля
3. Проверьте IP и порт (localhost:5432)

### Забыли пароль
```bash
# Сбросить пароль PostgreSQL
psql -U postgres
ALTER USER postgres WITH PASSWORD 'новый_пароль';
```

### База повреждена (внимание! только в крайнем случае)
```bash
# Резервная копия
pg_dump -U postgres shop > shop_backup.sql

# Восстановление
psql -U postgres shop < shop_backup.sql
```

---

*Выбирайте инструмент, который вам нравится больше!*
*Мой топ: 1️⃣ DBeaver, 2️⃣ pgAdmin, 3️⃣ консоль psql*
