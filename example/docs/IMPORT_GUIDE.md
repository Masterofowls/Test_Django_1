# 📦 Импорт товаров - Руководство

## Обзор

Скрипт `import_products.py` позволяет импортировать товары из CSV и XLSX файлов с автоматической коррекцией кодировки и поддержкой русского языка.

## Требования

Убедитесь, что установлены все зависимости:

```powershell
pip install -r requirements.txt
```

Основные модули:
- **openpyxl** - для работы с XLSX файлами
- **django** - веб-фреймворк

## Подготовка данных

### Формат CSV

Файл должен быть в одном из следующих форматов кодировки:
- UTF-8 (рекомендуется)
- UTF-8 with BOM
- CP1251 (Windows-1251)
- Latin-1
- ISO-8859-5

**Обязательные столбцы:**
- `Название` (или `name`, `Name`)
- `Артикул` (или `sku`, `SKU`)
- `Цена` (или `price`, `Price`)

**Опциональные столбцы:**
- `Описание` (или `description`, `Description`)

**Пример CSV файла (products.csv):**
```csv
Название,Артикул,Цена,Описание
Ноутбук Dell XPS,DELL-001,45000,Мощный ноутбук
Смартфон Samsung,SAM-001,25000,Современный смартфон
Наушники Sony,SONY-001,8000,Беспроводные наушники
```

**Разделители:** Скрипт автоматически определяет разделитель (запятая, точка с запятой, табуляция)

### Формат XLSX

**Требования:**
- Первая строка - заголовки столбцов
- Обязательные столбцы: Название, Артикул, Цена

**Поддерживаемые названия столбцов:**
- Название / Name
- Артикул / SKU
- Цена / Price
- Описание / Description

## Использование

### Шаг 1: Перейдите в директорию проекта

```powershell
cd D:\Test_Django_1\example\back
```

### Шаг 2: Активируйте виртуальное окружение

```powershell
venv\Scripts\activate
```

### Шаг 3: Запустите импорт

#### Из CSV файла:
```powershell
python import_products.py products.csv
```

#### Из XLSX файла:
```powershell
python import_products.py products.xlsx
```

#### С полным путем:
```powershell
python import_products.py "D:\files\products.csv"
python import_products.py "C:\Users\YourName\Downloads\products.xlsx"
```

## Примеры

### Пример 1: Импорт из встроенного шаблона

```powershell
# Отредактируйте products_template.csv своими товарами
python import_products.py products_template.csv
```

### Пример 2: Создание XLSX шаблона

```powershell
# Создаст products_template.xlsx с примерами
python create_xlsx_template.py

# Заполните свои данные и импортируйте
python import_products.py products_template.xlsx
```

### Пример 3: Обновление существующих товаров

Если товар с таким же артикулом (SKU) уже существует, его данные будут обновлены:

```powershell
# Первый импорт - создает товары
python import_products.py products_v1.csv

# Второй импорт - обновляет цены и описания
python import_products.py products_v2.csv
```

## Функции обработки данных

### Обработка кодировок

Скрипт автоматически определяет кодировку файла:
- UTF-8 ✓
- CP1251 (Windows-1251) ✓
- Latin-1 ✓
- ISO-8859-5 ✓

### Очистка данных

- Удаление пробелов в начале и конце
- Преобразование цены в формат Decimal
- Поддержка разных форматов цены: 1000.50, 1000,50

### Валидация

- Проверка обязательных полей (Название, Артикул, Цена)
- Проверка корректности цены
- Обнаружение дубликатов по артикулу

## Результаты импорта

После завершения импорта появится отчет:

```
============================================================
✅ Успешно импортировано: 15 товаров

⚠️  Ошибок: 2
  • Строка 5: Некорректная цена: abc
  • Строка 8: Отсутствуют обязательные поля
============================================================
```

## Порядок столбцов - ВАЖНО!

Порядок столбцов не важен! Скрипт автоматически найдет нужные столбцы по названиям:

✓ Эти порядки работают одинаково:
```
Название,Артикул,Цена,Описание
Артикул,Цена,Описание,Название
Цена,Описание,Название,Артикул
```

## Частые ошибки и решения

### Ошибка: "Файл не найден"
```
✗ Файл не найден: products.csv
```
**Решение:** Проверьте, что файл находится в текущей директории или укажите полный путь:
```powershell
python import_products.py "C:\Users\YourName\Downloads\products.csv"
```

### Ошибка: "Не найдены необходимые столбцы"
```
⚠️  Не найдены необходимые столбцы (Название, Артикул, Цена)
```
**Решение:** Убедитесь, что в файле есть столбцы с названиями:
- "Название" или "Name"
- "Артикул" или "SKU"  
- "Цена" или "Price"

### Ошибка при кодировке (?)
Скрипт попробует 5 разных кодировок. Если не поможет:
- Откройте CSV в Excel
- Сохраните как UTF-8: Файл → Параметры → Сохранить как → UTF-8

## Советы

1. **Перед импортом больших файлов:**
   - Сделайте резервную копию базы данных
   - Сначала импортируйте небольшой тестовый файл

2. **Формат цен:**
   - Используйте точку или запятую как разделитель
   - Примеры: 1000.50, 1000,50, 1000

3. **Артикулы должны быть уникальны:**
   - Избегайте дублей в файле
   - SKU используется для обновления существующих товаров

4. **Описание может быть пустым:**
   - Только Название, Артикул и Цена обязательны

## Тестирование

Используйте встроенный шаблон для быстрого теста:

```powershell
# 1. Создайте XLSX шаблон
python create_xlsx_template.py

# 2. Отредактируйте его (добавьте 1-2 товара)

# 3. Импортируйте
python import_products.py products_template.xlsx

# 4. Проверьте результаты в боковой стороне http://127.0.0.1:8000/
```

## Поддержка

Если возникли проблемы:
1. Посмотрите сообщение об ошибке в консоли
2. Проверьте формат файла
3. Убедитесь, что все данные в правильном кодировании (UTF-8)
