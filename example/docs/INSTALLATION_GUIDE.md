# 🚀 Инструкция по запуску проекта

Полностью готовый Django проект для демонстрационного экзамена.

---

## ⏱️ Время на установку: 5-10 минут

---

## 📋 Предварительные требования

✅ Python 3.10+  
✅ PostgreSQL установлена и запущена  
✅ Создана база данных `shop`

---

## 🟢 ШАГИ УСТАНОВКИ

### Шаг 1: Открыть терминал в папке проекта

```bash
cd example
```

### Шаг 2: Создать и активировать виртуальное окружение

**Windows (PowerShell):**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации начало строки должно начинаться с `(venv)`.

### Шаг 3: Установить зависимости

```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install django==6.0.1 psycopg2-binary openpyxl
```

**Проверка:**
```bash
python -m django --version
# Output: 6.0.1
```

### Шаг 4: Проверка конфигурации

```bash
python manage.py check
```

**Ожидаемый результат:**
```
System check identified no issues (0 silenced).
```

Если ошибка — проверить `config/settings.py` и параметры PostgreSQL.

### Шаг 5: Создание таблиц в БД

```bash
python manage.py makemigrations
python manage.py migrate
```

**Результат:**
```
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, sessions, bodies
Running migrations:
  ...
  Applying bodies.0001_initial... OK
```

### Шаг 6: Создание администратора

```bash
python manage.py createsuperuser
```

Ввести:
```
Username: admin
Email address: admin@example.com
Password: admin
Password (again): admin
```

Или используйте команду для создания всех тестовых пользователей сразу:

```bash
python manage.py create_test_users
```

Результат:
```
✓ АДМИНИСТРАТОР: admin / admin
✓ РЕДАКТОР: editor / editor
✓ АВТОРИЗИРОВАННЫЙ: user / user
✓ ГОСТЕВОЙ: guest / guest
```

### Шаг 7: Запуск сервера

```bash
python manage.py runserver
```

**Результат:**
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

⚠️ **Не закрывайте этот терминал!** Сервер должен работать в фоне.

---

## 📱 Проверка в браузере

Открыть следующие ссылки:

| URL | Что должно быть |
|-----|-----------------|
| http://127.0.0.1:8000/ | Каталог товаров (может быть пусто) |
| http://127.0.0.1:8000/register/ | Форма регистрации |
| http://127.0.0.1:8000/login/ | Форма входа |
| http://127.0.0.1:8000/admin/ | Админ-панель Django |

---

## 🔧 Админ-панель

1. Открыть http://127.0.0.1:8000/admin/
2. Логин: `admin`, Пароль: `admin`
3. Нажать **Sign in**

**На админ-панели можно:**
- Добавить товары (Products)
- Добавить пункты выдачи (Pickup Points)
- Просмотреть заказы (Orders)
- Управлять пользователями (Users)

---

## 📦 Добавление тестовых данных

### Вариант 1: Через админ-панель

1. На админ-панели нажать **Products → Add Product**
2. Заполнить форму:
   ```
   Name: iPhone 15
   Price: 99999.99
   SKU: SKU001
   Description: Смартфон Apple
   ```
3. Нажать **Save**
4. Повторить ещё 2-3 раза

### Вариант 2: Через импорт (если есть файл Tovar.xlsx)

```bash
python manage.py import_products "C:\path\to\Tovar.xlsx"
```

---

### Проверка работоспособности

#### Способ 1: Используя готовые тестовые аккаунты

Запустите команду:
```bash
python manage.py create_test_users
```

Затем войдите одной из готовых учетных записей:
- **admin** / admin (администратор со всеми правами)
- **editor** / editor (редактор для изменения товаров)
- **user** / user (обычный пользователь для заказов)
- **guest** / guest (гостевой пользователь без прав)

#### Способ 2: Создание нового пользователя вручную

1. Открыть http://127.0.0.1:8000/register/
2. Создать аккаунт:
   ```
   Username: testuser
   Password: test
   Confirm Password: test
   ```
3. Нажать **Зарегистрироваться**

4. Открыть http://127.0.0.1:8000/login/
5. Заполнить форму:
   ```
   Username: testuser
   Password: test
   ```
6. Нажать **Войти**

---

## 🆘 Решение ошибок

### ❌ `django.db.utils.OperationalError: could not translate host name "localhost"`

**Причина:** PostgreSQL не запущена или не установлена  
**Решение:**
```bash
# Убедиться что PostgreSQL запущена (Services → PostgreSQL на Windows)
# Или создать БД вручную в DBeaver
```

### ❌ `ModuleNotFoundError: No module named 'psycopg2'`

**Причина:** Не установлена библиотека PostgreSQL  
**Решение:**
```bash
pip install psycopg2-binary
```

### ❌ `TemplateDoesNotExist: products.html`

**Причина:** Шаблоны не созданы или находятся в неправильной папке  
**Решение:**
```bash
# Проверить что папка bodies/templates/ существует
# и внутри находятся: products.html, login.html, register.html, order_list.html
```

### ❌ Сервер не запускается

**Проверить:**
```bash
python manage.py check
# Должно вывести: System check identified no issues (0 silenced).
```

### ❌ БД не существует

```bash
# Создать БД вручную:
psql -U postgres -c "CREATE DATABASE shop;"
```

---

## 📊 Финальный чеклист

- [ ] Виртуальное окружение активировано (видно `(venv)`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `python manage.py check` выполняется без ошибок
- [ ] Миграции применены (`python manage.py migrate`)
- [ ] Суперпользователь создан (`python manage.py createsuperuser`)
- [ ] Сервер запускается (`python manage.py runserver`)
- [ ] http://127.0.0.1:8000/ открывается в браузере
- [ ] http://127.0.0.1:8000/admin/ открывается с логином
- [ ] Можно зарегистрировать пользователя
- [ ] Можно войти в систему
- [ ] Можно просмотреть заказы (будут пусты)
- [ ] Товары добавлены в админ-панели или импортированы

**Если все пункты отмечены ✅ — проект готов! 🎉**

---

## 🎓 Для демонстрации на экзамене

Показать комиссии:

1. Запустить сервер: `python manage.py runserver`
2. Открыть http://127.0.0.1:8000/ — **каталог товаров**
3. Открыть http://127.0.0.1:8000/register/ — **регистрация**
4. Создать аккаунт и войти
5. Открыть http://127.0.0.1:8000/orders/ — **история заказов**
6. Открыть http://127.0.0.1:8000/admin/ — **админ-панель**
7. Показать модели, товары, заказы, пользователей

---

**В проекте содержится всё необходимое для сдачи экзамена! ✅**
