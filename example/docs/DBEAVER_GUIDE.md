# 📊 Инструкция: Экспорт диаграммы БД в DBeaver

## 🎯 Быстрый старт (3 шага)

1. **Откройте DBeaver** и подключитесь к БД `shop`
2. **Щелкните правой кнопкой** на базе → **ER Diagram** → **Create Diagram**
3. **Экспортируйте** диаграмму: **File** → **Export** → **PNG/SVG**

## 🔗 Подключение к PostgreSQL базе

### Шаг 1: Открыть диалог подключения

**Способ 1:** Главное меню

- **File** → **New** → **Database Connection**

**Способ 2:** Быстро

- **Ctrl + Alt + N** (или нажать кнопку в левом боку)

**Способ 3:** Из View

- **Database** → **New Database Connection**

### Шаг 2: Выбрать PostgreSQL

![DBeaver Connection](https://imgur.com/placeholder.png)

1. Найти и выбрать **PostgreSQL**
2. Нажать **Next >**

### Шаг 3: Заполнить параметры подключения

| Параметр                | Значение | Пояснение                                   |
| ------------------------------- | ---------------- | ---------------------------------------------------- |
| **Server Name**           | MyShop           | Название подключения (любое) |
| **Server Host**           | localhost        | IP адрес PostgreSQL сервера              |
| **Server Port**           | 5432             | Порт PostgreSQL (по умолчанию)        |
| **Database**              | shop             | Имя базы данных                         |
| **Username**              | postgres         | Пользователь БД                        |
| **Password**              | postgres         | Пароль пользователя                |
| **Save password locally** | ☑               | Сохранить пароль                      |

**Окончательная форма:**

```
Server Name:    MyShop
Server Host:    localhost
Port:           5432
Database:       shop
Username:       postgres
Password:       [***] (сохранить)
```

### Шаг 4: Протестировать подключение

1. Нажать кнопку **Test Connection...**
2. Если появится окно загрузки драйверов:
   - Нажать **Download** (DBeaver загрузит PostgreSQL JDBC драйвер)
   - Дождаться завершения
3. Если появится сообщение **Connected** - успешно! ✅

### Шаг 5: Закончить настройку

1. Нажать **Finish**
2. Подключение появится в левой панели под **Database** → **MyShop**

---

## 🎨 Экспорт визуальной диаграммы БД

### Способ 1: Диаграмма всей БД (рекомендуется)

1. **Найти БД в левой панели**

   - Раскрыть: **Databases** → **shop**
2. **Создать диаграмму всей БД**

   - Клик правой кнопкой на **shop**
   - Выбрать **SQL Editor** → **New SQL Script**
   - Или: **ER Diagram** → **Create Diagram**
3. **Или используя меню:**

   - **Database** → **ER Diagram** → **Create diagram**
   - Или **Tools** → **ER Diagram** → **Create**
4. **В открывшемся окне диаграммы:**

   - Все таблицы будут отображены с связями
   - Можно переносить таблицы мышкой
   - Двойной клик на таблицу для редактирования

### Способ 2: Диаграмма одной таблицы

1. **Раскрыть таблицы в левой панели**

   - **Databases** → **shop** → **Schemas** → **public** → **Tables**
2. **Выбрать нужную таблицу**

   - Например: **bodies_order**
3. **Создать диаграмму**

   - Клик правой кнопкой на таблице
   - **Quick Diagram**
   - Или **View Diagram**

---

## 💾 Сохранение и экспорт диаграммы

### Сохранение в проект DBeaver

1. **В окне диаграммы нажать:**
   - **File** → **Save** (или Ctrl+S)
   - Диаграмма сохранится в проекте DBeaver

### Экспорт в файл (PNG, SVG, PDF)

#### Вариант 1: Через меню

1. **В окне диаграммы:**

   - **File** → **Export...**
2. **Выбрать формат:**

   - ☑ **PNG** - растровое изображение (jpg качество)
   - ☑ **SVG** - векторное (лучшее качество)
   - ☑ **PDF** - документ
3. **Выбрать путь для сохранения:**

   - Например: `d:\Test_Django_1\example\back\DB_SCHEMA.png`
4. **Нажать Export**

#### Вариант 2: Через скриншот

1. **Нажать** Screenshot button (камера)
2. **Выбрать область** диаграммы
3. **Сохранить** как PNG

#### Вариант 3: Скопировать в буфер обмена

1. **Ctrl + C** - скопировать всю диаграмму
2. **Вставить** в Word, PowerPoint, Paint и т.д.

---

## 🎓 Работа с диаграммой

### Навигация

| Действие               | Сочетание клавиш                            |
| ------------------------------ | ---------------------------------------------------------- |
| Приблизить           | Ctrl + Mouse Wheel (вверх)                            |
| Отдалить               | Ctrl + Mouse Wheel (вниз)                              |
| Вписать в окно     | Ctrl + 1                                                   |
| На 100%                      | Ctrl + 2                                                   |
| Панорамирование | Пространство + перемещение мыши |
| Выделить все        | Ctrl + A                                                   |

### Редактирование

| Действие                       | Как сделать                   |
| -------------------------------------- | --------------------------------------- |
| Переместить таблицу  | Перетащить мышкой       |
| Скрыть таблицу            | Ctrl + H                                |
| Показать все таблицы | Ctrl + A → Ctrl + E                    |
| Увеличить таблицу      | Правый клик → Expand Table   |
| Свернуть таблицу        | Правый клик → Collapse Table |

### Фильтрация таблиц

1. Нажать **Ctrl + F** (Search)
2. Введите часть названия таблицы
3. Диаграмма отобразит только подходящие таблицы

**Примеры:**

- `order` - покажет: bodies_order, bodies_order_products
- `user` - покажет: bodies_user, bodies_profile
- `product` - покажет только: bodies_product

---

## 🖼️ Экспорт в разные форматы

### PNG (растровое изображение)

**Плюсы:**

- ✅ Хорошее качество для документов
- ✅ Поддерживается везде
- ✅ Меньший размер файла

**Минусы:**

- ❌ Теряет качество при масштабировании

**Использование:** документация, презентации

### SVG (векторное изображение)

**Плюсы:**

- ✅ Идеальное качество при любом масштабировании
- ✅ Можно редактировать в векторных редакторах
- ✅ Меньше файла чем PNG

**Минусы:**

- ❌ Не все программы его поддерживают

**Использование:** веб-сайты, презентации, печать

### PDF (документ)

**Плюсы:**

- ✅ Универсальный формат
- ✅ Хорошо выглядит при печати
- ✅ Защита от редактирования

**Минусы:**

- ❌ Больше всего места на диск

**Использование:** печать, архивирование, отправка по email

---

## 📋 Наша схема БД

### Основные таблицы

```
bodies_user (Пользователи)
  ├─ id (PK)
  ├─ username (UK)
  ├─ password
  ├─ email (UK)
  └─ ... другие поля

bodies_profile (Профили с ролями)
  ├─ id (PK)
  ├─ user_id (FK → bodies_user) [1:1]
  └─ role (unauthorized|authorized|editor|admin)

bodies_product (Товары)
  ├─ id (PK)
  ├─ name
  ├─ price
  ├─ sku (UK)
  ├─ quantity
  └─ ... timestamps

bodies_pickuppoint (Точки выдачи)
  ├─ id (PK)
  ├─ name
  ├─ address
  └─ ... контактная информация

bodies_order (Заказы)
  ├─ id (PK)
  ├─ user_id (FK → bodies_user) [N:1]
  ├─ pickup_point_id (FK → bodies_pickuppoint) [N:1]
  ├─ status
  ├─ total_price
  └─ ... timestamps

bodies_order_products (Товары в заказах)
  ├─ id (PK)
  ├─ order_id (FK → bodies_order) [N:M]
  ├─ product_id (FK → bodies_product) [N:M]
  ├─ quantity
  └─ price
```

### Связи

```
USER ←→ PROFILE (1:1)
USER → ORDER (1:N)
PICKUPPOINT → ORDER (1:N)
ORDER ← → PRODUCT (M:N через ORDER_PRODUCTS)
```

---

## ⚙️ Полезные возможности DBeaver

### Просмотр данных в таблице

1. **Раскрыть таблицу** в левой панели
2. **Двойной клик** на таблице
3. Откроется редактор с данными

**Быстрые команды:**

- **Ctrl + F** - поиск в таблице
- **Ctrl + Shift + F** - расширенный фильтр
- **F2** - редактировать ячейку
- **Ctrl + N** - новая строка
- **Delete** - удалить строку

### Запуск SQL запросов

1. **Новый SQL скрипт:**

   - **File** → **New** → **SQL Script**
   - Или **Ctrl + Alt + Shift + S**
2. **Написать запрос:**

```sql
SELECT u.username, p.role, COUNT(o.id) as orders_count
FROM bodies_user u
LEFT JOIN bodies_profile p ON u.id = p.user_id
LEFT JOIN bodies_order o ON u.id = o.user_id
GROUP BY u.id, u.username, p.role
ORDER BY u.username;
```

3. **Выполнить:**
   - **Ctrl + Enter** (с курсором на строке запроса)
   - Или **Ctrl + Alt + X** (выполнить все)

### Экспорт данных

1. **Правый клик** на таблице
2. **Export Data**
3. **Выбрать формат:** CSV, JSON, SQL, Excel и т.д.

---

## 🐛 Решение проблем

### Ошибка: "Unable to connect to database"

**Решение:**

1. Проверить, запущена ли PostgreSQL
2. Убедиться, что параметры верные:

   - Host: `localhost`
   - Port: `5432`
   - Username: `postgres`
   - Password: правильный пароль
3. В терминале проверить:

```powershell
# Проверить, доступна ли БД
psql -h localhost -U postgres -d shop -c "SELECT version();"
```

### Диаграмма не отображает данные

**Решение:**

1. **Обновить схему:**

   - Правый клик на БД → **Refresh** (F5)
2. **Пересоздать диаграмму:**

   - Закрыть текущую
   - **Tools** → **ER Diagram** → **Create** (заново)

### Ошибка при экспорте

**Решение:**

1. Проверить путь для сохранения (должен быть доступен для записи)
2. Закрыть другие программы, использующие файл
3. Выбрать другой формат (PNG вместо SVG)

---

## 📚 Дополнительные ресурсы

- **Официальная документация DBeaver:** https://dbeaver.com/docs/
- **ER Diagram документация:** https://dbeaver.com/docs/wiki/ER-Model-Editor/
- **PostgreSQL в DBeaver:** https://dbeaver.com/docs/wiki/PostgreSQL-Configuration/

---

## ✅ Контрольный список

- [ ] DBeaver установлен
- [ ] PostgreSQL запущена
- [ ] Подключение к базе `shop` создано и протестировано
- [ ] ER диаграмма открывается для всей БД
- [ ] Диаграмма отображает все 6 таблиц и связи
- [ ] Диаграмма экспортирована в файл (PNG или SVG)
- [ ] Файл сохранен в проекте

---

## 🎬 Пошаговое видео-резюме

```
1️⃣ DBeaver → File → New Database Connection
   ↓
2️⃣ PostgreSQL → Host: localhost, Port: 5432, DB: shop
   ↓
3️⃣ Test Connection → Download Driver → Connected ✅
   ↓
4️⃣ Databases → shop → ER Diagram → Create Diagram
   ↓
5️⃣ Видим все таблицы с связями
   ↓
6️⃣ File → Export → PNG/SVG → Сохраняем файл
   ↓
7️⃣ Готово! 🎉
```

---

**Последнее обновление:** 2026-02-20
**Версия:** 1.0
**Статус:** ✅ Полная инструкция готова
