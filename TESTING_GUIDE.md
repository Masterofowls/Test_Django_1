# ✅ Быстрая проверка работоспособности проекта

Пошаговая инструкция как за 5-10 минут проверить что Django проект полностью рабочий.

---

## ⏱️ Время выполнения: 5-10 минут

---

## 📋 Что проверяем

1. ✅ Синтаксис и конфигурация
2. ✅ Подключение к БД
3. ✅ Запуск сервера
4. ✅ Доступность страниц
5. ✅ Функциональность (регистрация, заказы)

---

## 🟢 ЭТАП 1: Проверка конфигурации (2 мин)

### 1.1 — Проверить синтаксис проекта

```bash
python manage.py check
```

**✅ Если успешно:**
```
System check identified no issues (0 silenced).
```

**❌ Если ошибка:**
- Исправить ошибку в файле
- Повторить проверку

### 1.2 — Проверить наличие всех папок

```bash
# Убедиться что папки существуют
ls -la bodies/
ls -la bodies/templates/
ls -la bodies/migrations/
```

**Должны быть:**
```
bodies/
  ├── __init__.py
  ├── admin.py
  ├── models.py
  ├── views.py
  ├── urls.py ← ВАЖНО
  ├── migrations/
  │   ├── __init__.py
  │   └── 0001_initial.py ← Должна быть
  └── templates/
      ├── products.html ← ВАЖНО
      ├── login.html ← ВАЖНО
      ├── register.html ← ВАЖНО
      └── order_list.html ← ВАЖНО
```

---

## 🟡 ЭТАП 2: Проверка БД (2 мин)

### 2.1 — Проверить подключение к PostgreSQL

```bash
python manage.py dbshell
```

**✅ Если успешно:** откроется консоль PostgreSQL
```
shop=#
```

**Выход:**
```sql
\q
```

**❌ Если ошибка:** "could not connect"
- Проверить что PostgreSQL запущена
- Проверить параметры в `settings.py`

### 2.2 — Проверить таблицы в БД

```bash
python manage.py dbshell
```

Затем в консоли PostgreSQL:

```sql
-- Список таблиц
\dt

-- Должны быть таблицы:
-- auth_user
-- bodies_product
-- bodies_order
-- bodies_pickuppoint
-- bodies_profile
-- bodies_order_products

-- Проверить структуру таблицы Product
\d bodies_product

-- Выход
\q
```

**✅ Если успешно:** видны все таблицы  
**❌ Если ошибки:** запустить миграции:
```bash
python manage.py migrate
```

---

## 🔵 ЭТАП 3: Запуск сервера (1 мин)

### 3.1 — Открыть новый терминал

```bash
# Убедиться что виртуальное окружение активировано
# (venv) в начале строки

cd back
python manage.py runserver
```

**✅ Если успешно:**
```
Watching for file changes with StatReloader
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

**❌ Если ошибка:**
- Проверить наличие файлов
- Проверить `settings.py`
- Запустить `python manage.py check`

### 3.2 — Оставить сервер работать

⚠️ **Не закрывать этот терминал!** Сервер должен работать в фоне.

---

## 🟣 ЭТАП 4: Проверка страниц (3 мин)

Открыть браузер и проверить следующие адреса:

### 4.1 — Главная страница (каталог товаров)

```
http://127.0.0.1:8000/
```

**Что должно быть:**
- ✅ Заголовок "Каталог товаров" или похожий
- ✅ Кнопки "Вход" и "Регистрация" (если не авторизован)
- ✅ Список товаров (может быть пустым, но структура есть)

**Если ошибка 404 или 500:**
- Проверить что `bodies/urls.py` создан
- Проверить что в `back/urls.py` подключен `include('bodies.urls')`

### 4.2 — Страница регистрации

```
http://127.0.0.1:8000/register/
```

**Что должно быть:**
- ✅ Форма с полями "Username", "Password", "Password confirmation"
- ✅ Кнопка "Register" или "Зарегистрироваться"
- ✅ Ссылка "Уже есть аккаунт? Войдите"

**Если ошибка:**
- Проверить что `bodies/templates/register.html` создан
- Проверить что `bodies/views.py` содержит `register_view`

### 4.3 — Страница входа

```
http://127.0.0.1:8000/login/
```

**Что должно быть:**
- ✅ Форма с полями "Username", "Password"
- ✅ Кнопка "Sign in" или "Войти"
- ✅ Ссылка "Нет аккаунта? Зарегистрируйтесь"

### 4.4 — Админ-панель

```
http://127.0.0.1:8000/admin/
```

**Что должно быть:**
- ✅ Форма входа с полями Username и Password
- ✅ Сообщение о логине Django

**Если ошибка:**
- Проверить что создан суперпользователь: `python manage.py createsuperuser`

---

## 🟠 ЭТАП 5: Функциональное тестирование (3 мин)

### 5.1 — Создание пользователя (регистрация)

1. Открыть `http://127.0.0.1:8000/register/`
2. Заполнить форму:
   ```
   Username: testuser
   Password: testpass123
   Password (again): testpass123
   ```
3. Нажать **Register**

**✅ Если успешно:**
- Страница перенаправит на главную
- В верхнем углу будет написано "👤 testuser" или логин пользователя

**❌ Если ошибка:**
- Проверить что в `bodies/views.py` есть функция `register_view`
- Проверить что в `bodies/urls.py` указан маршрут для `register`

### 5.2 — Вход в систему

1. Нажать на ссылку **Logout** если вы вошли
2. Открыть `http://127.0.0.1:8000/login/`
3. Заполнить форму:
   ```
   Username: testuser
   Password: testpass123
   ```
4. Нажать **Sign in**

**✅ Если успешно:**
- Вы вернулись на главную страницу
- В углу видно "👤 testuser"

### 5.3 — Просмотр заказов (страница доступна только авторизованным)

1. Убедиться что вы вошли (видно имя пользователя)
2. Нажать на ссылку **Мои заказы** или открыть:
   ```
   http://127.0.0.1:8000/orders/
   ```

**✅ Если успешно:**
- Откроется страница с таблицей заказов
- Таблица пуста (если ещё нет заказов)

**❌ Если ошибка 404:**
- Проверить что в `bodies/urls.py` есть маршрут `path('orders/', ...)`
- Проверить что в `bodies/views.py` есть функция `order_list`

### 5.4 — Вход в админ-панель

1. Открыть `http://127.0.0.1:8000/admin/`
2. Войти с админом:
   ```
   Username: admin
   Password: (пароль который вы установили при createsuperuser)
   ```

**✅ Если успешно:**
- Откроется админ-панель Django
- Видны разделы: Users, Groups, Products, Orders, Pickup Points и т.д.

**❌ Если ошибка:**
- Создать суперпользователя: `python manage.py createsuperuser`

### 5.5 — Добавление тестовых товаров

1. В админ-панели нажать на **Products**
2. Нажать **Add Product**
3. Заполнить форму:
   ```
   Name: iPhone 15
   Price: 99999.99
   SKU: SKU001
   Description: Смартфон Apple
   ```
4. Нажать **Save**

**✅ Если успешно:**
- Товар появился в списке
- На главной странице видна товар

**❌ Если ошибка:**
- Проверить что модель `Product` зарегистрирована в `bodies/admin.py`

### 5.6 — Проверка импорта товаров (опционально)

Если нужно проверить импорт из Excel:

```bash
# Создать тестовый Excel файл с товарами
# Или использовать готовый Tovar.xlsx

python manage.py import_products "C:\path\to\Tovar.xlsx"
```

**✅ Если успешно:**
```
✅ Успешно импортировано 10 товаров
```

---

## 🟢 ЭТАП 6: Проверка БД через DBeaver (2 мин)

### 6.1 — Открыть DBeaver

1. Открыть DBeaver
2. Раскрыть подключение PostgreSQL
3. **Databases** → **shop** → **Schemas** → **public** → **Tables**

### 6.2 — Проверить таблицы

**Должны быть видны:**
- ✅ `auth_user` (содержит пользователей)
- ✅ `bodies_product` (содержит товары)
- ✅ `bodies_order` (содержит заказы)
- ✅ `bodies_pickuppoint` (пункты выдачи)
- ✅ `bodies_profile` (профили пользователей)

### 6.3 — Проверить данные

Двойной клик на таблицу `auth_user`:

**Должны быть:**
- ✅ Пользователь `admin` (суперпользователь)
- ✅ Пользователь `testuser` (тестовый)

Двойной клик на таблицу `bodies_product`:

**Должны быть:**
- ✅ iPhone 15 (если добавили в админ-панели)

---

## 🔴 КРИТИЧЕСКАЯ ПРОВЕРКА (обязательно!)

Если что-то не работает, следуйте этому чек-листу:

### ❌ Ошибка: 404 Page Not Found

```
✓ Убедиться что bodies/urls.py создан
✓ Убедиться что back/urls.py содержит include('bodies.urls')
✓ Перезагрузить браузер (Ctrl+F5)
✓ Перезапустить сервер (Ctrl+C и заново python manage.py runserver)
```

### ❌ Ошибка: TemplateDoesNotExist

```
✓ Убедиться что bodies/templates/ папка создана
✓ Убедиться что html файлы находятся в bodies/templates/
✓ Проверить правильность имён файлов (register.html, login.html и т.д.)
✓ Перезапустить сервер
```

### ❌ Ошибка: Database connection refused

```
✓ Проверить что PostgreSQL запущена (Services → PostgreSQL)
✓ Проверить параметры подключения в settings.py
✓ Убедиться что база shop создана в PostgreSQL
✓ Выполнить: python manage.py migrate
```

### ❌ Ошибка: relation "bodies_product" does not exist

```
✓ Выполнить: python manage.py makemigrations
✓ Выполнить: python manage.py migrate
✓ Проверить что таблицы видны в DBeaver
```

### ❌ Ошибка: ModuleNotFoundError: No module named 'psycopg2'

```
✓ Выполнить: pip install psycopg2-binary
✓ Убедиться что виртуальное окружение активировано
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

Отметьте все пункты:

### Конфигурация
- [ ] `python manage.py check` выполняется без ошибок
- [ ] Все папки созданы (templates, migrations, management/commands)
- [ ] Все файлы присутствуют (models.py, views.py, urls.py, admin.py)

### База данных
- [ ] PostgreSQL запущена
- [ ] База `shop` создана в PostgreSQL
- [ ] Таблицы созданы: `python manage.py migrate` выполнена
- [ ] Таблицы видны в DBeaver
- [ ] Суперпользователь создан: `python manage.py createsuperuser`

### Сервер
- [ ] Сервер запускается: `python manage.py runserver`
- [ ] Нет ошибок в консоли сервера
- [ ] Сервер доступен: `http://127.0.0.1:8000/`

### Страницы
- [ ] Главная страница открывается: `/`
- [ ] Страница регистрации открывается: `/register/`
- [ ] Страница входа открывается: `/login/`
- [ ] Админ-панель открывается: `/admin/`
- [ ] Страница заказов открывается (авторизованным): `/orders/`

### Функциональность
- [ ] Можно зарегистрировать пользователя
- [ ] Можно войти в систему
- [ ] Можно выйти из системы
- [ ] Можно добавить товар в админ-панели
- [ ] Товары видны на главной странице
- [ ] История заказов доступна авторизованным

### БД
- [ ] Пользователь видна в таблице `auth_user`
- [ ] Товар видна в таблице `bodies_product`
- [ ] В админ-панели зарегистрированы все модели
- [ ] SQL запросы выполняются в DBeaver

---

## 🎉 Если всё работает!

Проект **полностью рабочий** и готов к:
- ✅ Сдаче экзамена
- ✅ Нарезке видео-демонстрации
- ✅ Показу комиссии

---

## 📱 Краткая сводка за 1 экран

```bash
# Комбо для быстрой проверки:

# 1. Проверка конфигурации
python manage.py check

# 2. Проверка БД
python manage.py dbshell
\dt
\q

# 3. Миграции (если нужны)
python manage.py makemigrations
python manage.py migrate

# 4. Создание админа (если нужен)
python manage.py createsuperuser

# 5. Запуск сервера
python manage.py runserver

# 6. Тест в браузере
# http://127.0.0.1:8000/
# http://127.0.0.1:8000/admin/
# http://127.0.0.1:8000/register/
```

---

**Последнее обновление:** 19 февраля 2026 г.
