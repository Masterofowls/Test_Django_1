# Django E-commerce Exam Project

**Демонстрационный экзамен по специальности 09.02.07 — Инфраструктура информационных технологий**

Веб-приложение для заказа товаров с доставкой на пункты выдачи. Минимальный MVP согласно критериям.

---

## 🎯 Функциональность

- **Каталог товаров** — просмотр всех доступных товаров с ценой и артикулом
- **Аутентификация** — регистрация и вход пользователей
- **Создание заказов** — пользователи могут заказать товар в пункт выдачи
- **История заказов** — просмотр собственных заказов со статусом и кодом получения
- **Админ-панель** — управление товарами, заказами и пользователями
- **Импорт товаров** — команда для загрузки товаров из Excel

---

## 📋 Требования

- **Python** ≥ 3.10
- **Django** 6.0.1
- **PostgreSQL** 12+
- **DBeaver** (опционально, для управления БД)

---

## 🚀 Быстрый старт (5 минут)

### 1. Создание виртуального окружения

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install django==6.0.1 psycopg2-binary openpyxl
```

### 3. Создание базы данных

**Способ 1 — DBeaver (рекомендуется):**
1. Открыть DBeaver → New Connection → PostgreSQL
2. Host: `localhost`, Port: `5432`, User: `postgres`, Password: `postgres`
3. Test Connection → OK → Create → New Database → Name: `shop` → OK

**Способ 2 — Командная строка:**
```bash
psql -U postgres -c "CREATE DATABASE shop;"
```

### 4. Миграции БД

```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Создание администратора

```bash
python manage.py createsuperuser
# Username: admin
# Password: (ваш пароль)
```

### 6. Запуск сервера

```bash
python manage.py runserver
```

Открыть: `http://127.0.0.1:8000/`

---

## 📁 Структура проекта

```
back/
├── back/                          # Конфиг проекта
│   ├── settings.py               # Настройки Django
│   ├── urls.py                   # Главные маршруты
│   ├── wsgi.py                   # Production entry point
│   └── asgi.py
├── bodies/                        # Основное приложение
│   ├── models.py                 # Модели БД (Product, Order, PickupPoint, Profile)
│   ├── views.py                  # Представления (product_list, create_order...)
│   ├── urls.py                   # Маршруты приложения
│   ├── admin.py                  # Админ-панель
│   ├── apps.py
│   ├── templates/                # HTML-шаблоны
│   │   ├── products.html         # Список товаров
│   │   ├── login.html            # Форма входа
│   │   ├── register.html         # Форма регистрации
│   │   └── order_list.html       # История заказов
│   └── management/commands/      # Кастомные команды
│       └── import_products.py    # Импорт товаров из Excel
├── manage.py                      # Django CLI
├── db.sqlite3                    # База данных (разработка)
└── README.md                      # Этот файл
```

---

## 🗄️ Модели БД

### Product (Товар)
```python
- name: CharField(255)           # Название товара
- price: DecimalField(10, 2)     # Цена в руб.
- sku: CharField(50, unique)     # Артикул (уникальный)
- description: TextField         # Описание товара
```

### PickupPoint (Пункт выдачи)
```python
- address: CharField(500)        # Адрес пункта выдачи
```

### Order (Заказ)
```python
- user: ForeignKey(User)         # Пользователь, сделавший заказ
- products: ManyToManyField      # Товары в заказе
- createdAt: DateTimeField       # Дата создания заказа
- deliveryDate: DateTimeField    # Дата доставки (опционально)
- receiveCode: CharField(10)     # Код для получения заказа (генерируется автоматически)
- pickupPoint: ForeignKey        # Пункт выдачи
- status: CharField (new/delivered) # Статус заказа
```

### Profile (Профиль пользователя)
```python
- user: OneToOneField(User)       # Связь с пользователем
- role: CharField (admin/manager/customer) # Роль пользователя
```

---

## 🔗 URL-маршруты

| Маршрут | Метод | Назначение |
|---------|-------|-----------|
| `/` | GET | Список продуктов |
| `/register/` | GET, POST | Регистрация пользователя |
| `/login/` | GET, POST | Вход в систему |
| `/logout/` | GET | Выход из системы |
| `/orders/` | GET | История заказов пользователя |
| `/buy/<id_товара>/<id_пункта>/` | GET | Создать заказ |
| `/admin/` | GET | Админ-панель |

---

## 📊 Импорт товаров

### Формат Excel-файла (Tovar.xlsx)

| Артикул | Наименование товара | Цена | Описание товара | ... |
|---------|-------------------|------|-----------------|-----|
| SKU001 | Товар 1 | 100.50 | Описание | ... |
| SKU002 | Товар 2 | 250.00 | Описание | ... |

### Команда импорта

```bash
python manage.py import_products "путь/к/файлу.xlsx"
```

Пример:
```bash
python manage.py import_products "D:\exam\Tovar.xlsx"
```

**Результат:** Товары появятся в БД, существующие артикулы обновленные.

---

## 🔐 Аутентификация и авторизация

### Роли пользователей

1. **Администратор** (superuser)
   - Доступ к `/admin/`
   - Управление всеми моделями

2. **Менеджер** (через Profile.role)
   - Технически — обычный пользователь с ролью в Profile

3. **Покупатель** (default)
   - Может просмотреть товары
   - Может создать заказ
   - Может просмотреть свои заказы

### Защита представлений

```python
@login_required              # Требует авторизации
def order_list(request):
    orders = Order.objects.filter(user=request.user)
    return render(request, 'order_list.html', {'orders': orders})
```

---

## ⚙️ Конфигурация `settings.py`

### База данных

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',  # PostgreSQL
        'NAME': 'shop',
        'USER': 'postgres',
        'PASSWORD': 'postgres',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

### Установленные приложения

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'bodies',  # 👈 Наше приложение
]
```

---

## 🧪 Тестирование

### Создание тестовых данных вручную

1. Запустить сервер: `python manage.py runserver`
2. Перейти на `/admin/`
3. Добавить пункты выдачи:
   - Адрес 1: *ул. Пушкина, д. 1, Москва*
   - Адрес 2: *ул. Лермонтова, д. 5, Санкт-Петербург*
4. Добавить товары вручную или импортировать из Excel

### Тестирование функциональности

```bash
# 1. Проверить список товаров
curl http://127.0.0.1:8000/

# 2. Проверить возможность регистрации
# Открыть http://127.0.0.1:8000/register/

# 3. Проверить создание заказа
# Авторизоваться и нажать кнопку "Заказать"
```

---

## 🐛 Типичные ошибки и решения

### ❌ `django.db.utils.OperationalError: could not translate host name "localhost"`

**Причина:** PostgreSQL не запущен или база не создана

**Решение:**
```bash
# Убедиться что PostgreSQL работает
# Windows: Services → PostgreSQL
# Linux: sudo systemctl start postgresql
# Создать базу в DBeaver или через psql
```

### ❌ `ModuleNotFoundError: No module named 'psycopg2'`

**Причина:** Не установлена зависимость

**Решение:**
```bash
pip install psycopg2-binary
```

### ❌ `TemplateDoesNotExist: products.html`

**Причина:** Файл шаблона не создан в правильной папке

**Решение:**
```
Создать: bodies/templates/products.html
```

### ❌ `django.db.utils.ProgrammingError: column "receiveCode" of relation "bodies_order" already exists`

**Причина:** Миграция конфликтует с существующей структурой

**Решение:**
```bash
# Удалить миграции (кроме __init__.py) в bodies/migrations/
# Удалить базу: DROP DATABASE shop;
# Пересоздать: createdb shop
python manage.py makemigrations
python manage.py migrate
```

---

## 📱 Примеры использования API (для расширения)

### Получить все товары

```python
from bodies.models import Product

products = Product.objects.all()
for p in products:
    print(f"{p.name} — {p.price} руб. (SKU: {p.sku})")
```

### Создать заказ программно

```python
from bodies.models import Order, Product, PickupPoint
from django.contrib.auth.models import User

user = User.objects.get(username='john')
product = Product.objects.get(sku='SKU001')
pickup = PickupPoint.objects.get(id=1)

order = Order.objects.create(user=user, pickupPoint=pickup)
order.products.add(product)
order.save()
```

### Получить заказы пользователя

```python
orders = user.orders.all()
for order in orders:
    print(f"Заказ #{order.id}: {order.get_status_display()}, Код: {order.receiveCode}")
```

---

## 🔄 Миграции

### Создание миграции после изменения модели

```bash
python manage.py makemigrations
```

### Применение миграций

```bash
python manage.py migrate
```

### Просмотр статуса миграций

```bash
python manage.py showmigrations
```

---

## 📚 Полезные команды Django

```bash
# Интерактивная оболочка Python с Django
python manage.py shell

# Очистить кэш
python manage.py clear_cache

# Создать резервную копию БД
python manage.py dumpdata > backup.json

# Восстановить из резервной копии
python manage.py loaddata backup.json

# Проверить синтаксис проекта
python manage.py check
```

---

## 🎓 Критерии оценки (по заданию)

✅ **Требования, реализованные в проекте:**

- [x] Аутентификация пользователей (регистрация, вход, выход)
- [x] Каталог товаров с выводом основной информации
- [x] Система заказов с привязкой к пользователю
- [x] Пункты выдачи и привязка к заказам
- [x] Код получения заказа (автогенерируется)
- [x] Админ-панель для управления данными
- [x] Импорт товаров из внешних источников (Excel)
- [x] Минимальный, но функциональный фронтенд

---

## 📞 Справка по версиям

```
Django==6.0.1          # Веб-фреймворк
psycopg2-binary==2.9.* # PostgreSQL адаптер
Python==3.10+          # Интерпретатор
PostgreSQL==12+        # База данных
```

---

## 🔗 Ссылки

- [Django Documentation](https://docs.djangoproject.com/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [DBeaver Documentation](https://dbeaver.io/docs/)

---

**Создано для демонстрационного экзамена ИРПО (2026)**

**Последнее обновление:** 19 февраля 2026 г.
